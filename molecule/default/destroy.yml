---
- name: Destroy test container
  hosts: localhost
  gather_facts: false
  # no_log: "{{ molecule_no_log }}"

  tasks:
    - name: Remove Debian 13 container
      containers.podman.podman_container:
        name: molecule-debian13
        state: absent
