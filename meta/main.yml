---
galaxy_info:
  author: nipil
  description: >-
    Install the specified ssh public keys for the user and add an sshd config
    drop-in that only allows that user to log in via ssh, then reload sshd
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: Debian
      versions:
        - bookworm
        - trixie
    - name: Ubuntu
      versions:
        - jammy
        - noble
  galaxy_tags:
    - opencode
    - gitweb
dependencies: []
